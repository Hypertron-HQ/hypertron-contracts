[package]
name = "hypertron-compliance"
description = "Hypertron Privacy Protocol — optional exit-time allowlist policy (compliance hook)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
